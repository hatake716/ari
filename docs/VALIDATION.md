# 検証記録 — 1.3.0

2026年9月18日、室数上限の撤廃・巣形状・クリア後の母女王の観察継続を検証。

- `testDebugUnitTest`: **44件成功、失敗0**（既存生態24、暦・歩行6、巣の成長8、観察継続6）。
- `lintRelease`: **エラー0、警告40**。baselineで警告を隠していません。
- `assembleDebug`, `assembleDebugAndroidTest`, `assembleRelease`, `bundleRelease`: **成功**。
- 最終コードで専用API 35エミュレーターの操作・描画テストを全件実行し、**21件成功、失敗0、294.379秒**。

## 母女王の観察継続

- クリア後に「新たな女王アリを迎える巣を作る」と「現在の女王アリのその後を観察する」を縦に表示。両方の全文が見えることを実際のスクリーンショットで確認。
- 観察継続で、巣・女王室・障害物・働きアリ・通常の幼体・食料・経過日数・同じ保存枠を引き継ぐことを検証。旅立った子女王・雄を除き、次の育成が可能になることを確認。
- 保存枠の再読込とActivity再生成後も継続し、次の子女王の羽化・成熟・飛行を経て、2回目の選択画面と再度の観察再開まで操作テストに成功。UIの2回目は羽化直前の状態を設定した検証で、80日の発育全体はモデル検証が担当。
- モデルでは女王1匹の通常創設から、同じ巣で**3回の旅立ち**に到達（seed 812、入口に小枝・女王室手前に小石）。約1187.94日、1552.96日、1917.96日。各回の選択待ち中の停止と、再開後の保存復元による将来状態の一致を確認。
- 寿命10〜20年の範囲・保存後の不変性・寿命到達での終了・継続不可・旧クリアセーブの移行・留守中進行を検証。寿命の終幕はUIでも確認し、新しい巣の建設へ移れることを検証。
- 従来の新しい巣を作る操作、建設中断時の旧記録保持、ほかの保存枠の独立性、破損保存の保護も回帰テストに含みます。

[2つの選択肢の画面](screenshots/ending-two-choices.png)は検証用のクリア状態です。年齢・繁殖間隔は[BIOLOGY.md](BIOLOGY.md)に明記したゲーム用設定で、実在の個体の将来予測ではありません。

## 上限のない増室と巣の形

- 9種類の初期設計すべてで160室を完成させ、さらに着工できることを検証。元の部屋・女王室・障害物を保持し、入口から全室への経路を確認。
- 1024室まで増築して保存復元し、さらに20室増やした後も状態が一致。固定された室数・深さ上限がないこと、曲線通路の逆向き座標、未完成坑道の先端、拡大時の座標を検証。
- 512室の断面をAndroid Canvasで描画。巣材・内容物のキャッシュが表示画面の画素数以下で、拡大時も部屋の細部を描き直すことを確認。160室＋工事中の1室では、タップ・ピンチ・全体表示・保存再開・Activity再生成を操作テストで検証。
- 通常の巣づくりから、女王の到着、成長、外敵撃退、子女王の旅立ちまで、観察の早送りのみで到達する操作テストも成功。
- [同じ巣の成長画像](screenshots/natural-nest-growth.png)は、通常モデルを100日・400日・600日進め、実際の描画処理で出力。働きアリ16→192→1584匹、完成4→9→53室。800日には完成108室。室数を強制設定した画像ではありません。
- 商用利用・改変可能なCC BY 4.0の構造研究と公開3Dモデルの出典、作者、ライセンス、ゲーム用の変更を[NEST_SOURCES.md](NEST_SOURCES.md)とアプリ内に掲載。原図・モデルファイルの同梱はありません。

## 配布・旧版互換・実機

| 成果物 | サイズ | SHA-256 |
|---|---:|---|
| ARI-1.3.0.apk | 10,607,093 bytes | `0313448508bc2a69dcba1c72f1c9663484051ede4876c0b0c5b4bf2e41d05ac6` |
| ARI-1.3.0.aab | 10,572,349 bytes | `b55eb8537c7a21d348278d92cc44daaa756c44b5d2b9e4b8a52e3ff6a9fb26f6` |

applicationId `io.github.hatake716.ari`、versionName `1.3.0`、versionCode `5`。APKのv2署名、AABの署名、ZIP整合性、全14個の画像・音声アセットとソースの一致を検証。証明書SHA-256は従来と同じ `2fb270c7569bae6250005da1781db98be3befa63cc0a5b13e4a01595b843e4e6`。

専用エミュレーターで署名付き1.2.1に48室の旧セーブを用意し、1.3.0を `adb install --no-incremental -r` で更新。更新前後のJSONのSHA-256は `c19b403fc93f79805adf3738d692260dfd8fdfd507600971b91bdc9248670e68` で一致。更新後の観察画面に48室と表示され、早送り後に完成・工事中を合わせて**59室**へ増築することを確認。保存スキーマversion 1を維持し、新しい項目は旧データに既定値を適用します。

**Pixel 10a**を1.2.1から1.3.0へ同じ署名で上書き更新。インストール成功、versionCode 5、初回インストール日時の維持、端末内APKと配布APKのSHA-256一致、`Status: ok`の起動、プロセスと前面Activityを確認しました。実機ではデータ消去・アンインストール・自動操作テストを行っていません。操作・画面検証は専用エミュレーターで実施し、実機での長時間性能・BGMの聴感は未評価です。

- 検証ログ・比較画像・旧版更新証跡: ローカル `artifacts/validation-v1.3/`。
- 実機更新記録: ローカル `artifacts/device-delivery/pixel10a-1.3.0.json`。
- 署名付き配布物・チェックサム: ローカル `artifacts/release/v1.3.0/`。

Google Play Consoleへのアップロード・審査申請・公開は行っていません。

---

# 検証記録 — 1.2.1

2026年9月18日、エンディングから次の巣を建設する操作を検証。

- `testDebugUnitTest`: **36件成功、失敗0**。
- `lintRelease`: **エラー0、警告41**（既存の警告数と同じ）。
- `assembleDebug`, `assembleDebugAndroidTest`, `assembleRelease`, `bundleRelease`: **成功**。
- 専用API 35エミュレーターでエンディングに関する**3件の操作テストに成功（18.078秒）**。クリア・敗北の両方から「新たな女王アリを迎える巣を作る」で建設画面へ移り、小枝の配置と新しい女王の到着まで確認。
- 終了済みの保存枠を開き直しても同じ操作ができること、建設を中断すると元の終了記録を保持すること、新しい女王を迎えたときに選択中の枠だけを置き換えること、ほかの2枠の保存内容が一致することを確認。「遊び方と生態」の「観察に戻る」は従来どおり動作。
- エンディングの長いボタン名が表示されることと、遷移後の建設画面をスクリーンショットで確認。今回の操作検証は上記3件で、女王の創設から旅立ちまでの長時間シナリオは1.2.0の検証記録を参照。

| 成果物 | サイズ | SHA-256 |
|---|---:|---|
| ARI-1.2.1.apk | 10,603,833 bytes | `f02a924fa97d7146ac1e74d14a8506ed1a3765ca1f5836b326ba9e80170e3053` |
| ARI-1.2.1.aab | 10,569,760 bytes | `aec956305552eb53d497577608d76cf0f65260c017ac9e5bd5742b1638b7370b` |

applicationId `io.github.hatake716.ari`、versionName `1.2.1`、versionCode `4`。APK・AABの署名を検証。証明書SHA-256は前版と同じ `2fb270c7569bae6250005da1781db98be3befa63cc0a5b13e4a01595b843e4e6`。保存形式は変更していません。

**Pixel 10a**を1.2.0から1.2.1へ `adb install --no-incremental -r` で上書き更新。インストール成功、バージョン、初回インストール日時の維持、端末内APKと配布APKのSHA-256一致、`Status: ok`での起動と前面表示を確認。実機のデータ消去・アンインストール・自動操作テストは行っていません。

- 検証ログと画面: ローカル `artifacts/validation-v1.2.1/`。
- 実機更新記録: ローカル `artifacts/device-delivery/pixel10a-1.2.1.json`。
- 配布物・チェックサム: ローカル `artifacts/release/v1.2.1/`。

Google Play Consoleへのアップロード・審査申請・公開は行っていません。

---

# 検証記録 — 1.2.0

2026年9月18日、JDK 17 / Android SDK 36 / 専用API 35エミュレーター（emulator-5554）で検証。

## 成長・描画・操作

- `testDebugUnitTest`: **36件成功、失敗0**。生態24件、暦と歩行6件、増室・掘削・深い巣の保存・カメラ座標6件。
- `lintRelease`: **エラー0、警告41**。描画時の割り当て、KTXへの置換提案、既存の文字列・縦画面など。baselineで隠していません。
- `assembleRelease`, `bundleRelease`, `assembleDebug`, `assembleDebugAndroidTest`: **成功**。
- エミュレーターで**17種類の操作・描画テストに成功**。回帰14件（225.030秒）、最終の描画・増室・停止4件（21.297秒、停止テスト1件は重複）を実行。
- 3つの巣形状×3つの大きさのすべてで、48室まで増室できることを確認。元の部屋・女王室・障害物を保存し、すべての部屋へ経路が通ること、未完成の掘削先端からも働きアリが戻れることを検証。
- 通路を掘ってから部屋を広げる工程、工事途中の保存復元とその後の一致、冬・防衛中の労働配分、元の観察範囲より深い座標へのタップとパンを検証。
- 32室＋掘削78%の部屋を用意し、部屋の詳細表示、ピンチ後の倍率変更、全体表示への復帰、保存枠を開き直す操作、Activity再生成後の進捗一致を確認。画面を確認して拡大の実変化を検証。
- 外敵3種類×24姿勢を実際のCanvasで描画。種類ごとの異なる歩行姿勢と、脚・触角・尾鉗が画像端で切れないことを画素検査。
- 通常の巣づくりから、女王の到着、働きアリの増加、48室への拡張、外敵撃退、子女王の旅立ちまで早送りだけで到達。別の決定的モデル検証（seed 812）でも1067日でクリア、働きアリ6672匹、子女王12匹、完成48室。
- 大きな巣の停止中にも毎フレーム描画していた箇所を修正。静止した土粒・幼体・食料をキャッシュし、一時停止中は変化があるときだけ描き直します。最終コードで停止時の画素一致と拡大・復元を再確認しました。

同じ巣の自然な成長例（seed 812、入口の小枝＋女王室手前の小石、モデル内日数）:

| 経過日数 | 働きアリ | 女王・幼体を含む群れ | 完成した部屋 |
|---:|---:|---:|---:|
| 100日 | 16匹 | 56匹 | 4室 |
| 200日 | 44匹 | 130匹 | 5室 |
| 400日 | 192匹 | 582匹 | 9室 |
| 600日 | 1584匹 | 3179匹 | 48室 |

特定の実在種を測定した結果ではなく、本作のモデルと設計値による検証です。画面の働きアリは最大240匹の代表表示です。

## 配布・保存互換性・実機

| 成果物 | サイズ | SHA-256 |
|---|---:|---|
| ARI-1.2.0.apk | 10,603,641 bytes | `19d794388eacd6f83b96d51c6ca8af699fba75fc59df5a09cf9e6540ba25d7fc` |
| ARI-1.2.0.aab | 10,569,263 bytes | `51c25021d205889a456113e5047e478a6a46422832294ee1680f03e874c4c493` |

applicationId `io.github.hatake716.ari`、versionName `1.2.0`、versionCode `3`、minSdk 26 / targetSdk 36。APKのv2署名、AABの署名、ZIP整合性、全画像・音声の同梱内容を検証。証明書SHA-256は旧版と同じ `2fb270c7569bae6250005da1781db98be3befa63cc0a5b13e4a01595b843e4e6`。

専用エミュレーターで署名付き1.1.0に創設途中の旧セーブを置き、1.2.0を `adb install --no-incremental -r` で上書き更新。更新前後の保存JSONのSHA-256はともに `55b10d1737afca3e06ab3470881edec89fc507d2112e65c5575a80bb4a504a60` で、内容が完全一致しました。更新後に枠を開き、巣の創設・4室・5月の暦表示が再開することも確認しています。旧セーブの早送り設定はそのまま続きます。

接続中の **Pixel 10a** も署名付き1.1.0から1.2.0へ上書き更新し、インストール成功、versionCode 3、初回インストール日時の維持、端末内APKと配布APKのSHA-256一致、`Status: ok` の起動、アプリの前面表示を確認。実機ではデータ消去・アンインストール・自動操作テストは行っていません。実機の長時間の見た目・性能・BGMの聴感についてはユーザー確認が別途必要です。

Google Play Consoleへのアップロード・審査申請・公開は行っていません。

- 検証ログと旧版更新証跡: ローカル `artifacts/validation-v1.2/`。
- 実機更新の記録: ローカル `artifacts/device-delivery/pixel10a-1.2.0.json`。
- 配布物とチェックサム: ローカル `artifacts/release/v1.2.0/`。
- 画面: [外敵の形態](screenshots/enemy-models.png)、[同じ巣の成長](screenshots/colony-growth.png)、[32室の操作検証](screenshots/expanded-ui.png)。比較図もゲームと同じ描画処理で出力しています。

---

# 検証記録 — 1.1.0

2026年9月18日、JDK 17 / Android SDK 36 / 専用API 35エミュレーター（emulator-5554）で検証。

## ビルド・モデル・操作

- `testDebugUnitTest`: **30件成功、失敗0**（既存24件＋暦と歩行の6件）。全月境界、2月28日、年越し、365日周期、従来スキーマの保存復元、留守中の月越し、三脚の位相、障害物と歩調、短い方向への旋回を確認。
- `lintRelease`: **エラー0、警告33**。更新提案、縦画面、描画時の割り当て、文字列など。baselineによる非表示なし。
- `assembleRelease`, `bundleRelease`, `assembleDebug`, `assembleDebugAndroidTest`: **成功**。
- 専用エミュレーターの `AndroidJUnitRunner`: **14件成功、失敗0、141.407秒**（既存GameFlowTest 10件＋VisualSeasonTest 4件）。
- 巣づくりから女王1匹で始め、小枝・小石を配置して7日/秒で観察する通常プレイが、外敵撃退・巣の拡張・子女王の旅立ちまで成功。保存されたCLEARED状態、働きアリ・子女王・撃退数・容量増加を検証。
- 5月31日→6月1日の実際の早送り、1月15日の保存枠を再開しActivityを再生成した後の暦表示、12種類の異なる背景画像、最大2枚の背景キャッシュを確認。
- 同一の描画エンジンから働きアリ・女王・有翅女王の比較画像を出力し、全24歩行ポーズの描画を確認。一時停止時とゲーム終了後に脚・触角を含む画像が完全に停止することを画素比較で検証。
- 初期実装では触角の逐次描画がエミュレーター上で約22fpsに低下したため、16ポーズの独立キャッシュへ変更。最終コードで通常プレイと全操作テストをやり直して成功。最終観察ログはおおむね28〜29fps。実機性能の測定ではありません。

## 配布と旧版の更新

| 成果物 | サイズ | SHA-256 |
|---|---:|---|
| ARI-1.1.0.apk | 10,589,845 bytes | `7448b74248882572cc545de886b20a62a186bb586f957879667f6ed3769c8f09` |
| ARI-1.1.0.aab | 10,541,037 bytes | `e08ae16ba29ec811919bc87aadd8d8a17f3007444c5c55e4129333276bcbda19` |

applicationId `io.github.hatake716.ari`、versionName `1.1.0`、versionCode `2`、minSdk 26 / targetSdk 36。APKはv2署名を検証し、AABは `jarsigner -verify` で検証。証明書SHA-256は1.0.0と同じ `2fb270c7569bae6250005da1781db98be3befa63cc0a5b13e4a01595b843e4e6`。APK/AAB内の12枚のWebPがソースと一致することを確認。オフラインで季節を表示できます。

専用エミュレーターに署名付き1.0.0をインストールし、旧版で作成済みの創設14.1875日目のセーブを用意した後、署名付き1.1.0を `adb install -r` で上書き更新しました。インストール前後で保存JSONが完全一致し、更新後に巣を開くと「1年目 5月15日」の観察画面に戻ることを確認。保存スキーマはversion 1を維持しています。

操作検証後、アプリ内解説の日本語の属名を「クロオオアリなどのオオアリ属」に訂正し、releaseを再生成、lint・署名・12枚の同梱物を再検証しました。描画・保存・生態・操作のコードに変更はありません。上のチェックサムは訂正後の配布物です。

生成画の全プロンプトと月別対応は [ART.md](ART.md) と [season-artwork.json](season-artwork.json)、モデルの参考資料と簡略化の範囲は [BIOLOGY.md](BIOLOGY.md) に記録。

- 実行ログ・署名検証・更新証跡: ローカルの `artifacts/validation-v1.1/`。
- 配布コピーとチェックサム: `artifacts/release/v1.1.0/`。
- 描画の比較: [アリ](screenshots/ant-models.png)、[12か月](screenshots/twelve-months.png)、[冬の観察画面](screenshots/winter-ui.png)。モデル比較と冬画面は検証用状態です。
- 日常利用の実機へのインストールや実機の見た目・性能・聴感は未確認。Google Playへのアップロード・審査申請・公開も行っていません。

---

# 検証記録 — 1.0.0

2026年9月17日、JDK 17 / Android SDK 36 / API 35 Androidエミュレーター（emulator-5554）で検証。

## 自動テストとビルド

- `testDebugUnitTest`: **24件成功、失敗0**。
- `lintRelease`: **エラー0、警告34**。警告はライブラリ更新案、縦画面指定、描画中の割り当て、KTX推奨、日本語文字列の組み立てなど。lint baselineで隠していません。
- `assembleRelease`, `bundleRelease`, `assembleDebug`, `assembleDebugAndroidTest`: 成功。
- `connectedDebugAndroidTest`: **9件成功、失敗0**。巣の作成、小枝・小石の実際の配置、女王室変更、部屋追加、保存・再開、3枠の独立性、Activity再生成、ピンチ、記録閲覧、襲撃、敗北、旅立ち、破損データ保護、BGM設定・バックグラウンド復帰を検証。
- さらに `naturalColonyFromEditorReachesFlightWithoutPlayerIntervention` で、新規巣づくり→女王1匹→7日/秒の観察→クリアまでの通常プレイを検証。**成功（95.567秒、失敗0）**。上の9件と合わせてエミュレーターで10件のシナリオを検証しました。

単体テストは発育段階と時間、季節、寿命、経路、障害物の局所効果、採餌への不利益、防衛不足の敗北、防衛成功、拡張、成熟/過密による繁殖、ゲーム終了後の停止、戦闘中の保存復元、乱数継続、フレーム分割による結果の一致、留守中の1倍時間と30日上限を含みます。

自然成長のモデルテスト（seed=812、樹形・ふつう、小枝と小石を各1個）では約481.98日でクリアし、働きアリ814匹、子女王2匹、襲撃3回を撃退、部屋6室を確認しました。これは一つの乱数・設計条件の結果で、すべての巣の生存を保証するものではありません。

## 配布物の検証

| 成果物 | サイズ | SHA-256 |
|---|---:|---|
| 署名付きAPK | 4,236,337 bytes | `e279e69b7c3d47fde66bc15a4a239563a8fec899f9ad8d80a648df73449ac262` |
| 署名付きAAB | 4,183,475 bytes | `a3da47f678090e0c40e44abd84ef2811c07fd41fec32009e699eb5c031f0343f` |

APKは `apksigner verify` でv2署名・RSA 3072bitの署名者1名を検証。証明書SHA-256は `2fb270c7569bae6250005da1781db98be3befa63cc0a5b13e4a01595b843e4e6`。AABは `jarsigner -verify` で検証済み（Androidアプリ用の自己署名証明書）。

`aapt dump badging` で applicationId `io.github.hatake716.ari`、versionName `1.0.0`、versionCode `1`、minSdk 26、targetSdk 36を確認。インターネット権限なし。APK/AAB両方に背景画像とBGMが同梱されています。BGMは64秒、22,050Hz、ステレオVorbis、合成時最大振幅0.70。

## 確認範囲

- エミュレーターの操作結果と保存後のモデル状態を確認しています。単なるビルドやインストール成功を操作テストとして扱っていません。
- UIテストの敗北・旅立ち・侵入・多数個体の描画は、対象場面の直前の状態を用意したテストです。これとは別に女王1匹からの通常プレイでもクリアを確認しています。
- 描画はおおむね30fpsを目標にしています。エミュレーターではアリの歩行フレームのキャッシュ導入後に約28〜31fpsのログを確認しました。実機の性能・消費電力測定ではありません。
- 実機へのインストール、実機での見た目や操作感、スピーカーでのBGM聴感は未確認です。
- Google Play Consoleへのアップロード、審査申請、ストア公開は行っていません。

## 通常プレイの完了結果

巣の編集画面から小枝・小石を各1個配置し、女王を迎え、「7日/秒」を選んだ後は巣に介入せず観察しました。約481.98日でクリア、働きアリ814匹、子女王2匹、撃退3回、部屋6室、母女王の健康100%を保存データと画面で確認しました。

署名付きrelease APKを専用エミュレーターにインストールし、新規の巣づくり、女王を迎える操作、観察画面、バックグラウンドからの復帰を確認。インストール済みbase.apkのSHA-256は配布APKと一致し、releaseプロセスのクラッシュログはありません。
